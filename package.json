{
  "name": "cotulenh-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build --filter='!web-one'",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "check-types": "turbo run check-types",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.5.0",
    "prettier-plugin-svelte": "^3.3.3",
    "turbo": "^2.5.3"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
